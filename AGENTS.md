# AGENTS.md - Workspace 使用規則

這份文件是本 workspace 的入口規則。  
AI 進入這個資料夾工作時，請先讀本檔，再依任務讀各專案或共用資料夾內的導覽文件。

## 0. Workspace 定位

此資料夾是生前的創作與開發工作區，不只放程式 project，也放：

- 作品企劃
- 世界觀設定
- 劇本／小說／文本
- 角色與視覺素材
- Live2D、Ren'Py、LoRA 等開發資料
- AI 助理自身的共用設定與形象素材
- 待整理資料

根目錄目前命名為 `Workspace`，代表這裡是創作、開發、設定與協作資料的共同工作區。

## 1. 根目錄分區

根目錄原則上只放「workspace 層級資料夾」、「作品／專案資料夾」與少量入口規則檔，不要直接散放素材或文本。

建議結構：

```text
00_Admin        AI 助理、工作區管理、非作品本體的行政資料
00_ToSort       臨時收件匣，尚未判斷歸屬的資料
00_共用參考     跨作品、跨專案共用的參考資料
10_作品與專案   所有作品／專案資料夾的統一入口
AGENTS.md       本規則檔
```

根目錄的 `00_` 是「workspace 層級的入口／管理／共用區」，不是作品排序。  
作品或專案統一放進 `10_作品與專案`；作品／專案資料夾本身原則上**不加編號**，直接用作品名或專案名。

### 00_Admin

放「管理 AI／workspace 本身」的資料，不放作品正式內容。

適合放：

- AI 助理頭像、banner、形象素材
- Discord 使用素材
- AI 角色身份設定的輔助資料
- workspace 管理文件
- 非作品本體的操作規則或行政資料

目前例子：

```text
00_Admin\AI形象素材\澪
00_Admin\AI形象素材\沐晴
00_Admin\AI形象素材\凜
00_Admin\AI形象素材\合照
00_Admin\OpenClaw-Watchdog
00_Admin\OpenClaw-Watchdog-Rin
```

### 00_ToSort

臨時收件匣。只有在「還不知道該放哪」時使用。

規則：

- 可以暫放，但不要長期堆積。
- 整理時應移到明確的作品、共用參考或 Admin。
- 不確定歸屬時，可以先建立子資料夾並加日期或簡短說明。

建議命名：

```text
00_ToSort\2026-05-14_待判斷來源素材
00_ToSort\臨時截圖
```

### 00_共用參考

放「跨作品／跨專案會共同使用」的資料。  
不只放世界觀，也可以放任何共用參考，但需要分類清楚。

適合放：

- 世界觀總覽與規則
- 跨作品角色／勢力／時間線
- 共用名詞表
- 共用美術風格規範
- 共用技術流程
- OpenClaw、AI 協作、升版等共用操作參考
- 多作品共用素材規格

不適合放：

- 單一作品限定文本
- 單一作品限定素材
- 某個 project 的開發原始碼
- 還沒判斷用途的雜物（先放 `00_ToSort`）

目前實際子分類（2026-07-10 校正）：

```text
00_共用參考\世界觀\暫名世界一          世界觀資料（暫名；未來世界二、世界三比照新增）
00_共用參考\操作流程\AI協作            跨 AI 協作流程（含 Discord規則範本）
00_共用參考\AI\skills                  AI 共用 skill（如 project-creator）
```

未來若新增「素材規格」等共用分類，直接在本層建立，並同步更新本節。

## 2. 作品／專案資料夾規則

每個作品或專案應獨立成一個資料夾，統一放在：

```text
10_作品與專案
```

例：

```text
10_作品與專案\驚蟄
10_作品與專案\黑貓槍手與小公主魔法師
10_作品與專案\Live2D_Rin
```

作品／專案資料夾本身原則上不加編號。  
如果要把某作品做成遊戲、視覺小說、Live2D 或其他可執行成果，仍然放在該作品資料夾內；不要另外在 workspace 根目錄開一個脫離作品的資料夾。

一般作品建議結構：

```text
00_導覽與規則   入口、開發規則、閱讀順序、接續方式
01_文本         劇本、小說、台詞、企劃文字
02_素材         圖像、音訊、角色素材、背景、生成素材
03_開發         程式、Ren'Py、Live2D、ComfyUI/LoRA 工作資料等
04_共用參考     此作品內部共用但不一定跨作品的設定或參考
05_成品         匯出版本、可交付成果
99_封存         舊版、廢案、備份、暫停使用資料
```

不是所有專案都必須完整使用上述資料夾；但如果資料開始變多，優先照這套結構整理。  
若專案使用自訂編號（例如 `Virtual_Avatar_Studio` 的 `04_輸出版本`），必須在該專案的 `AGENTS.md` 內提供與本模板的編號對照，避免 AI 依全域編號慣例放錯位置。

### 00_導覽與規則

AI 接手作品時第一個讀的地方。

應包含：

- `開發規則.md`
- `00_接續摘要.md`（接續摘要統一使用此檔名，2026-07-10 起全作品適用）
- 閱讀順序
- 目前進度
- 重要限制
- 生前偏好的處理方式

### 01_文本

放正式或半正式文字內容。

適合放：

- 小說原稿
- 劇本
- 對話
- 章節草稿
- 文案

### 02_素材

放創作素材。

適合放：

- 角色立繪
- 背景
- 表情差分
- 音訊
- 圖像參考
- 生成圖
- 素材清單

### 03_開發

放會被工具或引擎使用的開發資料。

適合放：

- Ren'Py project
- 視覺小說專案本體
- Live2D project
- LoRA 訓練 workspace
- ComfyUI workflow
- 程式碼
- debug log

例：如果要把《驚蟄》做成視覺小說，Ren'Py 專案本體應放在：

```text
10_作品與專案\驚蟄\03_開發\JingZhe
```

而不是另開根目錄專案。

### 04_共用參考

放「此作品內部共用」的資料。

例如：

- 角色關係表
- 場景設定
- 風格規範
- 名詞表
- 分支設計

如果資料會跨多個作品共用，應移到根目錄 `00_共用參考`。

### 05_成品

放可交付或已輸出的結果。

例如：

- build
- zip
- release
- 可玩版本
- 完稿版本
- 對外展示圖

例：如果《驚蟄》視覺小說輸出可玩版本，應放在：

```text
10_作品與專案\驚蟄\05_成品
```

### 99_封存

放舊版、廢案、暫時不用但不想刪的資料。

規則：

- 優先封存，不要直接刪除。
- 封存資料夾或檔名盡量附原因或日期。

## 3. 世界觀資料規則

世界觀不等於作品。  
一個世界觀底下可以有多個作品；未來也可能有世界二、世界三。

世界觀資料建議放在：

```text
00_共用參考\世界觀\暫名世界一
00_共用參考\世界觀\暫名世界二
```

目前既有世界一資料位於：

```text
00_共用參考\世界觀\暫名世界一
```

使用規則：

- 「世界一」只是暫名，不是正式名稱。
- 不要把世界一規則自動套用到世界二、世界三。
- 新作品是否屬於世界一，要先確認，不要自動歸類。
- 世界共用設定放世界觀資料夾；作品限定設定放作品資料夾。

當生前說：

> 去看世界一的總覽

AI 應先讀世界一的總覽文件，再繼續任務。

## 4. 路徑書寫規則

為了讓澪、沐晴、凜能共用同一套文件，workspace 內的規則、總覽、索引、教學文件，原則上不要硬寫某一台設備的絕對路徑。

### 抽象根路徑

統一使用：

```text
@/workspace
```

代表「該 AI 自己設備上的創作／開發 workspace」。

其中：

- `@/`：每位 AI 自行定義的本機根路徑。
- `@/workspace`：該 AI 自己設備上的 workspace。

例如澪目前可理解為：

```text
@/ = C:\Users\User\OneDrive
@/workspace = C:\Users\User\OneDrive\Workspace
```

但沐晴、凜或其他 AI 必須改用自己的實際路徑，不可直接照抄澪的絕對路徑。

### 文件中應優先這樣寫

```text
@/workspace/AGENTS.md
@/workspace/00_共用參考/世界觀/暫名世界一
@/workspace/10_作品與專案/驚蟄/03_開發/JingZhe
```

### 什麼時候可以寫絕對路徑

只有在以下情況才寫絕對路徑：

- 文件明確只給某一位 AI／某一台設備使用。
- 記錄本機工具、模型、服務位置，例如 ComfyUI、Ollama、Python venv。
- debug log 或錯誤訊息本身包含絕對路徑。

即使寫絕對路徑，也要標明這是「某台設備的本機路徑」，不要當成共用路徑。

## 5. 命名規則

### 編號

常用編號意義：

```text
00_ 入口、管理、導覽、共用規則
01_ 主要文字／主資料
02_ 素材
03_ 開發
04_ 共用參考
05_ 成品
99_ 封存
```

同層資料夾可以有多個 `00_`，但名稱必須清楚，例如：

```text
00_Admin
00_ToSort
00_共用參考
```

### 檔名

建議：

- 用中文可讀名稱，不必硬翻英文。
- 重要入口檔可用 `00_` 開頭。
- 暫名要明寫 `暫名`。
- 版本可用 `v1`, `v2` 或日期。

例：

```text
世界觀總覽_暫名世界一.md
開發規則.md
00_接續摘要.md
2026-05-14_整理前備份
```

### 避免

- 不明確的 `new`, `final`, `final_final`
- 無日期的雜物資料夾
- 把不同作品素材混在同一層
- 把跨作品設定塞進單一作品資料夾

## 6. 專案／作品自己的 AGENTS.md

根目錄的 `AGENTS.md` 定義整個 workspace 的通用規則。  
如果某個作品或專案有自己的特殊規則，可以在該作品資料夾內再建立專屬 `AGENTS.md`。

建議使用時機：

- 該作品有固定語氣、格式、禁忌或世界觀限制
- 該專案有特殊開發流程
- AI 接手時需要先知道該作品怎麼運作
- 作品規模變大，單靠 `00_導覽與規則` 不夠清楚

讀取順序：

1. workspace 根目錄 `AGENTS.md`
2. 作品／專案根目錄 `AGENTS.md`（如果存在）
3. 該作品的 `00_導覽與規則`
4. 任務需要的文本、素材、開發或世界觀資料

目前不強制每個作品都建立專屬 `AGENTS.md`；等作品規則變多時再補。

## 7. AI 工作規則

AI 在此 workspace 工作時：

1. 先讀根目錄 `AGENTS.md`。
2. 若進入特定作品，先看該作品是否有自己的 `AGENTS.md`。
3. 再讀該作品 `00_導覽與規則`。
4. 若涉及世界觀，讀對應世界觀總覽。
5. 若尚未建立 Discord 相關工作規範，先到 `@/workspace/00_共用參考/操作流程/AI協作/Discord規則範本/` 複製範本，再改寫成自己的正式規則。
6. 若不確定資料歸屬，先提出建議，不要擅自大搬家。
7. 搬動或改名大量檔案前，先列出計畫給生前確認。
8. 新增、刪除、改名或搬動資料夾／檔案後（包含檔名變更、資料夾名變更、路徑調整），應從異動位置一路往上檢查每個祖先層資料夾的 `AGENTS.md`（包含父層、祖父層直到 workspace 根目錄），確認是否需要同步更新導覽、索引、命名規則、路徑引用或資料夾／檔案說明。若被改名或搬動的是其他 `AGENTS.md`、索引、導覽或頻道規則明確引用的入口檔，必須同步更新所有已知引用；若無法立即更新，應保留相容的轉址／說明檔或記錄待修正項。
9. 讀取 AGENTS、索引、導覽、頻道規則或其他規則文件時，若文件引用的檔案或路徑不存在，不要直接判定失敗；應先依引用用途、檔名語意與所在資料夾尋找同用途的新入口檔（例如 AGENTS、README、索引、總覽、接續摘要、角色設定、世界觀主檔等），並回報該引用可能需要更新。
10. 不要刪除資料；需要移除時優先放 `99_封存`。
11. 涉及本地工具、模型、服務時，要確認是哪台設備／哪位 AI 的環境。
12. 進入 workspace 工作時，若發現 `00_ToSort` 內有存放超過 14 天的項目，應主動列出並向生前提出歸檔建議；不要放著不管，也不要未經確認就自行歸檔。

## 8. 版本控制、同步與備份

### 版本控制現況

- 本 workspace 目前**不使用 git**（2026-07-10 已移除失效的空 `.git` 資料夾）。
- AI 不要假設此處是 git repo，也不要擅自 `git init`。
- 未來若要導入 git，先與生前確認策略。建議方向：各開發專案（`03_開發` 內的工程資料夾）各自建 repo，而不是整個 workspace 一個大 repo；且要留意 git 與 OneDrive 同步互相干擾的風險（本次 `.git` 失效即為案例）。

### 不需同步／備份的生成快取

以下是工具自動生成、可隨時重生的快取，不需備份，未來若上 git 應加入 `.gitignore`；新增同類資料時比照辦理：

- Godot：`.godot/`
- Ren'Py：`game/cache/`、`*.rpyc`、`*.rpyb`、`log.txt`、`errors.txt`、`traceback.txt`
- LoRA／訓練：tensorboard 事件檔、可重生的中間產物
- 通用：`node_modules/`、`__pycache__/`、venv 等

OneDrive 無法單獨排除子資料夾同步。目前快取量小（後室 `.godot` 約 1MB），先接受同步；若之後某個開發專案的快取變大，再考慮把該工程資料夾移出 OneDrive，並在原位置的 `03_開發` 留說明檔記錄實際位置。

### 備份策略

- 各作品 `01_文本` 與 `00_共用參考\世界觀` 是不可再生資料，不能只依賴 OneDrive 同步。
- 建議節奏：每月一次打包 zip，存到 OneDrive 以外的位置（另一顆硬碟或另一個雲端）。
- 每次執行後，將日期記錄在 `00_Admin` 的管理文件中。

## 9. 目前待整理事項

- ~~之後可補一個「建立新作品／新專案」專用 skill。~~ 已完成：`00_共用參考\AI\skills\project-creator`。
- ~~後室行走模擬器原型尚無專屬 `AGENTS.md`。~~ 已補齊（2026-07-10）；目前五個專案皆有專屬 `AGENTS.md`，新專案建立時應一併建立。
- ~~`.agents`、`.codex` 空資料夾來源待確認。~~ 確認為空後已於 2026-07-10 經生前同意移除。
- 備份策略（見第 8 節）尚未實際執行第一次。
