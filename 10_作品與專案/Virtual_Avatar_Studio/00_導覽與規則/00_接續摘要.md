# Virtual_Avatar_Studio - 接續摘要

> 初版由澪於 2026-07-10 依現有資料整理；之後有實質進度時請直接更新本檔。

## 目前狀態

- 本專案是所有虛擬形象開發的共用工作區，不是單一角色專案。
- 目前唯一角色：`Rin`（原 `Live2D_Rin` 專案內容已移入 `01_角色/Rin/`）。
- 各角色的詳細進度以角色資料夾內的 `README.md` 為準（見 `00_導覽與規則/開發規則.md`）。

## 結構提醒

本專案使用自訂編號，與全域作品模板的對照如下：

| 本專案 | 對應全域模板 |
|---|---|
| 01_角色 | 01（主資料） |
| 02_共用素材 | 02_素材 |
| 03_開發工具與流程 | 03_開發 |
| 04_輸出版本 | 05_成品 |

注意：本專案的 `04_輸出版本` 是「成品」性質，不是全域編號的 `04_共用參考`。

## 下一步建議

1. 替 `01_角色/Rin` 補上或確認 `README.md`（角色定位、最新版本、素材來源、輸出位置、待辦）。
2. 已定稿的頭像／banner／桌寵素材與 `@/workspace/00_Admin/AI形象素材` 之間的基準版本，以角色 README 記錄。
