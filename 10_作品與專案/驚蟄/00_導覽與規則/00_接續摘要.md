# 驚蟄 - 接續摘要

> 初版由澪於 2026-07-10 依現有資料整理；之後有實質進度時請直接更新本檔。

## 目前狀態

- 《驚蟄》Ren'Py 全年齡版初版 demo 已建立，位於 `03_開發/JingZhe`。
- 2026-08-04：已完成 P1 全數 18 張透明立繪，並同步導入 `game/images/characters` 與 `game/placeholders.rpy`；正式角色資料夾只留透明 PNG，綠幕原圖統一封存於 `99_封存/2026-08-04_P1立繪綠幕原圖`；P2 仍待製作。
- 內容涵蓋序章～第六章；第五章的 R18 段落已明確跳過，保留全年齡版銜接提示。
- 驚蟄 v1.0 的日常立繪已依道具狀態分流：`jingzhe neutral` 使用 `jingzhe_daily_calm_3q_thigh_no_kiseru.png`，`jingzhe smoking` 使用 `jingzhe_daily_calm_3q_thigh_holding_kiseru_no_smoke.png`；現有正式角色狀態均以等義 ASCII 名稱導入 `game/images/characters`，未完成狀態仍以色塊 placeholder 代替（`game/placeholders.rpy`）。
- 劇本內立繪演出已依情節重排為語意化狀態，並補齊單人／雙人／三人站位；`game/script.rpy` 的 `show`／`hide` 為後續立繪清單的來源。
- 世界觀與「伊洛世界」有連動，詳見本專案 `AGENTS.md`。

## 素材現況（02_素材）

- 主角「驚蟄」v0.1 立繪組已完成：全身／半身／特寫多表情多姿勢（含拿菸斗差分），另有單獨的菸斗道具圖。
- 驚蟄 v0.2 於 2026-08-03 定案為全數不可直接用於遊戲，只保留作為視覺參考；正式遊戲立繪從 v1.0 起必須是透明背景 PNG 並遵守可讀檔名規格。
- 「花見」v0.1 表情差分 11 張。
- 「夢見」目前僅 1 張（微笑）。
- LoRA 訓練工作區：`02_素材/驚蟄/LoRA/jingzhe-v0.1-workspace`（含 config 與 tensorboard 紀錄）。
- 視覺鎖定清單：`02_素材/視覺鎖定清單/驚蟄.txt`。
- 絡新婦於 2026-08-06 完成 rev.2 主視覺重構：固定八肢接點、上下副足走向、下副足托衣襬、純白蛛甲本體、遮眼棉帽與長白無垢；截至 2026-08-07，`watchful`、`searching`、`attack`、`enraged` 四個劇本狀態皆已完成 v2.0 正式透明立繪並導入遊戲。
- 立繪需求帳本：`02_素材/立繪清單.txt` 已於 2026-08-03 依 `game/script.rpy` 重寫為劇本實際標籤版，涵蓋 7 名角色、45 個唯一狀態與 185 次 `show`，並記錄用途、首次出現、使用次數與 P0～P2 製作優先級。

## 下一步建議（沿用 03_開發/README.txt）

1. 補第七章分支結構。
2. 把角色立繪／背景替換進 image 定義。
3. 補音樂、音效與章節選單。
4. 為 Steam 版整理全年齡文本與內容標示。

## 整理紀錄

- 2026-08-03：校正角色素材版本邏輯與實體路徑：原 `v1` 改為 `v0.1`、原 `v2` 改為 `v0.2`、原 `v3` 改為 `v1.0`；花見既有第一輪素材亦改列 `v0.1`。LoRA 舊工作區與固定提示詞同步採 v0.1／v0.2 命名；立繪帳本與視覺鎖定文件自身版本改用 `rev`，避免和素材版本混淆。
- 2026-08-03：正式素材庫立繪檔名改採 `角色_情境情緒_鏡頭構圖_道具狀態.png`，遊戲鏡像改採等義 ASCII 名稱；將 v1.0 的 `045.png`、`049.png` 改為可直接辨識情境的名稱並同步 image 引用，同時定案 v0.2 全數不得直接進遊戲；原 `game/images/characters/jingzhe_test_thigh_3q.png` 因不透明且僅屬測試圖，移至 `99_封存/2026-08-03_非透明測試立繪`。
- 2026-08-03：完成第二階段立繪清單重寫；移除未在劇本使用的假想需求，改以 45 個實際 image 狀態為唯一帳本，補齊用途、首次出現、使用次數、優先級、素材現況與同步規則。
- 2026-07-30：導入驚蟄 v1.0 原 `049.png`（現名 `驚蟄_日常平靜_四分之三側身大腿構圖_持菸斗無煙.png`）作為遊戲預設立繪；重寫全劇立繪演出註記，拆分日常、受控、警戒、戰鬥、負傷、悲痛與真結局狀態。
- 2026-07-10：`03_開發` 內舊 log（errors.txt、traceback.txt、舊 RenPy 啟動紀錄）移至 `99_封存/2026-07-10_開發舊log`；散落的立繪 `69bffe7e58eed.png`（驚蟄全身拿菸斗）移至 `02_素材/驚蟄/`。
